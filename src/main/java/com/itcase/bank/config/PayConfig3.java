package com.itcase.bank.config;

/**
 * name:${name};
 * time:${time};
 * 用处：
 */
public class PayConfig3 {
//方法三：
//    直接将@ConfigurationProperties(prefix = "pay.nopassword")放在PaymentProperties 的类名上
}
